import 'package:flutter/material.dart';
import '../components/constants.dart';

class MyFamiliesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Families',
        style: optionStyle,
      ),
    );
  }
}
